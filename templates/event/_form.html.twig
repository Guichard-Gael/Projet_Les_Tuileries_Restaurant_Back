
{{ form_start(form) }}
    {{ form_widget(form) }}

    {% if is_granted('ROLE_ADMIN') %}
        <button class="btn btn-success my-3">{{ button_label|default('Enregistrer') }}</button>
    {% else %}
        <a class ="btn btn-secondary my-2" href="">Bouton d'enregistrement désactivé</a>
    {% endif %}

{{ form_end(form) }}
